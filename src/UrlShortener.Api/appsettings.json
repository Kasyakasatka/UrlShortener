{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cassandra": {
    "ContactPoints": [ "cassandra" ], // Matches the service name in docker-compose.yml 
    "Keyspace": "url_shortener"
  }
}
